name: Avatar v0.8 Isolated Speech Acceptance

on:
  push:
    branches: [avatar-showcase-v0.8]
    paths:
      - 'avatar-v07/index.html'
      - 'avatar-v07/styles.css'
      - 'avatar-v07/src/**'
      - 'avatar-v07/scripts/speech-acceptance.mjs'
      - 'avatar-v07/scripts/static-server.mjs'
      - '.github/workflows/avatar-v08-isolated-acceptance.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  acceptance:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      AVATAR_URL: http://127.0.0.1:3000
      PORT: 3000
      HOST: 127.0.0.1
    steps:
      - name: Checkout v0.8 branch
        uses: actions/checkout@v4
        with:
          ref: avatar-showcase-v0.8

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
        working-directory: avatar-v07

      - name: Start isolated static server
        run: |
          node scripts/static-server.mjs > v08-isolated-server.log 2>&1 &
          echo $! > v08-isolated-server.pid
          for attempt in $(seq 1 30); do
            if curl --fail --silent http://127.0.0.1:3000/studio >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat v08-isolated-server.log
          exit 1
        working-directory: avatar-v07

      - name: Run browser TTS acceptance
        run: node scripts/speech-acceptance.mjs
        working-directory: avatar-v07

      - name: Upload speech acceptance evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: avatar-v08-isolated-speech-acceptance
          path: |
            avatar-v07/avatar-v08-speech-artifacts/
            avatar-v07/v08-isolated-server.log
          if-no-files-found: error
          retention-days: 14

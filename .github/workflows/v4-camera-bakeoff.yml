name: V4 Camera Bake-off

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - v4-camera-bakeoff.mjs
      - .github/workflows/v4-camera-bakeoff.yml

jobs:
  compare:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Chromium
        run: |
          npm init -y
          npm install playwright
          npx playwright install --with-deps chromium
      - name: Compare Alicia closeup cameras
        run: node v4-camera-bakeoff.mjs
      - name: Upload camera comparison
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vrm-v4-camera-bakeoff
          path: v4-camera-bakeoff
          retention-days: 14
